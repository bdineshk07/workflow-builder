export default function App() {
  return (
    <div style={{ padding: 32, fontFamily: "system-ui, sans-serif" }}>
      <h1>AI Planet — Frontend scaffold</h1>
      <p>If you see this, the frontend dev server is running.</p>
      <p>Dev server: <code>npm run dev</code> (usually on http://localhost:5173)</p>
    </div>
  );
}
